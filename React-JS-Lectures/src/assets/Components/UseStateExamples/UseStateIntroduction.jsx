export default function UseStateIntroduction() {
  
  // state : mevcut değer
  // setState : değeri güncelleyen fonksiyon
  // initialValue : başlangıç değeri (sayı, string, bool, array, obj olabilir)
  // const [state, setState] = useState(initialValue);

  return (
    <>
      <h1>Test</h1>
    </>
  )
}